name: firebase_phone_auth_handler
description: An easy to use firebase phone authentication library to easily send and verify OTP's.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^1.1.1
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: